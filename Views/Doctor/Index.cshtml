@model IList<AcunMedyaHospitalProject.Entities.Doctor>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card shadow-sm">
        <div class="card-body">
            <h4 class="card-title text-primary">Doktor Tablosu</h4>
            <p class="card-description text-muted">
                Doktorların bilgileri bu alanda gözükecektir.
            </p>
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead style="background-color: #007bff; color: white;">
                        <tr>
                            <th>ID</th>
                            <th>İsim</th>
                            <th>Resim</th>
                            <th>Bölümü</th>
                            <th>Randevular</th>
                            <th>Güncelle</th>
                            <th>Kaldır</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ID</td>
                                <td>@item.FullName</td>
                                <td><img src="@item.ImgUrl" class="img-fluid img-thumbnail doctor-img" alt="Doctor Image" /></td>
                                <td>@item.Department.Name</td>
                                <td><a class="btn btn-info btn-sm" href="/Appointment/AppointmentByDoctor/@item.ID">Randevular</a></td>
                                <td><a class="btn btn-success btn-sm" href="/Doctor/UpdateDoctor/@item.ID">Güncelle</a></td>
                                <td><a class="btn btn-danger btn-sm" href="/Doctor/DeleteDoctor/@item.ID">Kaldır</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="mt-3">
                    <a class="btn btn-primary" href="/Doctor/CreateDoctor">Doktor Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 10px;
    }

    .card-title {
        font-weight: bold;
    }

    .table {
        border-radius: 5px;
        overflow: hidden;
    }

    .btn {
        border-radius: 20px;
    }

    .img-thumbnail {
        max-width: 300px; /* Daha büyük resim boyutu */
        max-height: 300px; /* Daha büyük resim boyutu */
        border-radius: 10px;
    }

    .doctor-img {
        max-width: 300px; /* Resimlerin genişliğini belirleyin */
        max-height: 300px; /* Resimlerin yüksekliğini belirleyin */
    }

    .thead-dark {
        background-color: #007bff !important; /* Siyah yerine mavi renk */
        color: white !important; /* Yazı rengini beyaz yap */
    }

    .table-hover tbody tr:hover {
        background-color: #f1f1f1; /* Hover için gri ton */
    }
</style>
